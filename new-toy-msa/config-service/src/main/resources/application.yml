server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git: #default
          uri: file:///Users/kimchanghee/이직준비/git-local-repo
          default-label: master