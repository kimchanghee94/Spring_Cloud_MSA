server:
  port: 8888

spring:
  application:
    name: config-service
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/kimchanghee/이직준비/git-local-repo
        git: #default
          uri: https://github.com/kimchanghee94/Spring_Cloud_Config
          default-label: main
#          username: [username]
#          password: [password]

management:
  endpoints:
    web:
      exposure:
        include: heath, busrefresh